# 暮澜纪元插件项目专属规则

本文件是本项目专属规则，仅适用于本插件目录下的开发工作。通用规则见根目录 AGENTS.md。

---

## 技术选型规则

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 选型基于事实 | [P0] | 技术选型（包名/框架/模式/库）必基于本项目实际情况（Bukkit 插件、中文团队、已用中文包名跑通、两插件物理隔离部署）做具体风险分析，禁套用"大厂规范"等普适模板。推荐方案时必说明"在本项目里，这个选择的具体风险是什么"，而非"大厂都这么做" |
| 决策不反复 | [P0] | 用户已确认的决策，除非有新的实质性风险证据，不得在同一会话里二次提出反对。风险提示一次到位，用户确认后即锁定 |

## 项目架构约束

- 公共模块包名：`暮澜纪元.通用`
- 公共模块的类不实现 `Serializable`
- 两个插件（XRM / XRMdenglu）运行时不通过反射互传公共模块类型
- 构建方式：Gradle composite build，根目录放公共模块 `xrmm-common`
- 依赖注入：公共模块引入 Guice，两个插件均用 Guice
- 公共模块边界：只放纯工具与接口（关键词颜色/解析器/格式化器/YAML 加载器/翻译服务接口/消息服务接口），业务实现留在各插件

## XRM 插件核心开发铁律

以下规则是 XRM 插件开发的最高准则，每次写代码前必须过一遍。

### 需求文档驱动开发 [P0]

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 以需求文档为准 | [P0] | 所有开发必须基于 `开发需求文档` 文件夹中的所有内容进行。需求文档是唯一事实来源，代码实现必须与需求文档一致 |
| 需求优先但必更新文档 | [P0] | 当用户需求与需求文档描述不一致时，以用户需求为准；但不论是冲突、新增、还是文档中未覆盖的内容，都必须同步地对需求文档进行添加/更新/更改/删除。即：不能只按用户反馈改代码，而是根据反馈先改需求文档，再按需求文档改代码 |
| 反馈不全必深挖 | [P0] | 用户的反馈仅基于实测，问题大概率不全。必须查看完整代码、理清逻辑、测试实际效果，把问题的根源修复。需求文档中写入的内容也要经过改动，改成合理的、适合 AI Agent 阅读的开发文档 |

### 日志追溯 [P0]

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 日志必有据 | [P0] | 所有问题必须能在 `D:\xuanr\Desktop\燃烧之陨我的世界服务端\暮澜纪元我的世界MMORPG服务端\plugins\XRM\logs` 中最后一次的 logs 里找到。如果找不到，说明该部分缺少 debug 日志，必须按照需求文档规范补充详细的 debug 日志 |
| 日志全覆盖 | [P0] | 所有关键路径（请求进入、分支判断、数据变更、异常捕获、响应返回）必须有 debug 级别日志，确保任何问题都能从日志中追溯完整调用链 |

### 循环工程自查 [P0]

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 举一反三排查 | [P0] | 任何 bug、改动、问题被提出后，必须通过循环工程 skill 的提问板块，自行思考所有可能出现的同类型错误，逐一排查项目中所有类似模式、类似逻辑是否存在相同隐患。有则修复，无则记录。禁止只修复用户指出的单点问题 |

### 依赖版本最新 [P0]

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 版本必最新 | [P0] | 插件所用的所有环境、所有依赖、所有可自选版本号的配置，必须使用最新构建版本 |
| 版本验证 | [P0] | 每次开发前，针对所有可自选版本号的配置，必须联网搜索确认版本号是否已更新。Gradle 依赖、Bukkit API、JDK 等均在检查范围内 |

## Git 上传规则

| 规则 | 优先级 | 内容 |
| --- | --- | --- |
| 重构后必上传 | [P0] | 每次进行重构或更改代码后，都必须上传 git，禁遗留未提交的改动堆积 |
| 使用总控制台上传 | [P0] | 上传 github 必须运行 `D:\xuanr\Desktop\燃烧之陨我的世界服务端\总控制台\总控制台.py`，通过该程序完成上传，禁直接手动执行 git 命令绕过总控制台 |
| 上传前自检 | [P1] | 上传前确认：编译通过、测试通过、无临时/调试残留文件；失败则先修复再上传 |
| 上传失败必报 | [P0] | 总控制台上传失败时立即停下，报告错误信息，禁自行重试或绕过总控制台用其他方式推送 |
