# 资源定义配置
# 定义游戏中所有专精资源（配置驱动，不再硬编码枚举）
# 字段说明：
#   名称: 资源的显示名称（用于日志和UI）
#   默认上限: 资源的最大值（受修饰器影响，修饰器类别: 资源_<标识>_上限）
#   每秒恢复: 每秒自动恢复的数值（0表示不自动恢复，由资源变更服务.恢复方法处理）
#   显示键: 翻译文件中的显示键（用于计分板/UI显示）

resources:
  秘能:
    名称: "秘能"
    默认上限: 4
    每秒恢复: 0
    显示键: "resource.arcane_charge"
