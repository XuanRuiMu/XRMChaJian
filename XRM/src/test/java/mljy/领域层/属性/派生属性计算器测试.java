package mljy.领域层.属性;

import org.junit.jupiter.api.*;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;

import java.util.function.DoubleUnaryOperator;

import static org.junit.jupiter.api.Assertions.*;

@DisplayName("派生属性计算器")
class 派生属性计算器测试 {

    private static final DoubleUnaryOperator 恒等递减 = d -> d;
    private static final DoubleUnaryOperator 归零递减 = d -> 0;
    private static final DoubleUnaryOperator 减半递减 = d -> d * 0.5;

    @Nested
    @DisplayName("全能增伤比例计算器")
    class 全能增伤比例计算器组 {

        @Test
        @DisplayName("派生名应返回'全能增伤比例'")
        void 派生名_应返回全能增伤比例() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            assertEquals("全能增伤比例", 计算器.派生名());
        }

        @Test
        @DisplayName("全能20点(恒等递减)→增伤比例0.10")
        void 全能20点_恒等递减_应返回0点10() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(20, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.10, 结果, 0.000001);
        }

        @Test
        @DisplayName("全能0点→增伤比例0")
        void 全能0点_应返回0() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("全能100点(减半递减)→有效50点→增伤比例0.25")
        void 全能100点_减半递减_应返回0点25() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(100, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 减半递减);

            assertEquals(0.25, 结果, 0.000001);
        }

        @Test
        @DisplayName("归零递减应始终返回0")
        void 全能任意_归零递减_应返回0() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(999, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 归零递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("负数全能应返回负增伤比例")
        void 负数全能_应返回负值() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(-20, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(-0.10, 结果, 0.000001);
        }

        @Test
        @DisplayName("NaN全能(恒等递减)应传播NaN")
        void NaN全能_应传播NaN() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(Double.NaN, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertTrue(Double.isNaN(结果));
        }
    }

    @Nested
    @DisplayName("全能减伤比例计算器")
    class 全能减伤比例计算器组 {

        @Test
        @DisplayName("派生名应返回'全能减伤比例'")
        void 派生名_应返回全能减伤比例() {
            派生属性计算器.全能减伤比例计算器 计算器 = new 派生属性计算器.全能减伤比例计算器();
            assertEquals("全能减伤比例", 计算器.派生名());
        }

        @Test
        @DisplayName("全能20点(恒等递减)→减伤比例0.10")
        void 全能20点_恒等递减_应返回0点10() {
            派生属性计算器.全能减伤比例计算器 计算器 = new 派生属性计算器.全能减伤比例计算器();
            属性快照 属性 = 创建属性快照(20, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.10, 结果, 0.000001);
        }

        @Test
        @DisplayName("全能0点→减伤比例0")
        void 全能0点_应返回0() {
            派生属性计算器.全能减伤比例计算器 计算器 = new 派生属性计算器.全能减伤比例计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("全能50点(减半递减)→有效25点→减伤比例0.125")
        void 全能50点_减半递减_应返回0点125() {
            派生属性计算器.全能减伤比例计算器 计算器 = new 派生属性计算器.全能减伤比例计算器();
            属性快照 属性 = 创建属性快照(50, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 减半递减);

            assertEquals(0.125, 结果, 0.000001);
        }
    }

    @Nested
    @DisplayName("吸血比例计算器")
    class 吸血比例计算器组 {

        @Test
        @DisplayName("派生名应返回'吸血比例'")
        void 派生名_应返回吸血比例() {
            派生属性计算器.吸血比例计算器 计算器 = new 派生属性计算器.吸血比例计算器();
            assertEquals("吸血比例", 计算器.派生名());
        }

        @Test
        @DisplayName("吸血15点(恒等递减)→吸血比例0.15")
        void 吸血15点_恒等递减_应返回0点15() {
            派生属性计算器.吸血比例计算器 计算器 = new 派生属性计算器.吸血比例计算器();
            属性快照 属性 = 创建属性快照(0, 15, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.15, 结果, 0.000001);
        }

        @Test
        @DisplayName("吸血0点→吸血比例0")
        void 吸血0点_应返回0() {
            派生属性计算器.吸血比例计算器 计算器 = new 派生属性计算器.吸血比例计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("吸血100点(归零递减)→有效0点→吸血比例0")
        void 吸血100点_归零递减_应返回0() {
            派生属性计算器.吸血比例计算器 计算器 = new 派生属性计算器.吸血比例计算器();
            属性快照 属性 = 创建属性快照(0, 100, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 归零递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("负数吸血应返回负比例")
        void 负数吸血_应返回负值() {
            派生属性计算器.吸血比例计算器 计算器 = new 派生属性计算器.吸血比例计算器();
            属性快照 属性 = 创建属性快照(0, -10, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(-0.10, 结果, 0.000001);
        }
    }

    @Nested
    @DisplayName("躲闪几率计算器")
    class 躲闪几率计算器组 {

        @Test
        @DisplayName("派生名应返回'躲闪几率'")
        void 派生名_应返回躲闪几率() {
            派生属性计算器.躲闪几率计算器 计算器 = new 派生属性计算器.躲闪几率计算器();
            assertEquals("躲闪几率", 计算器.派生名());
        }

        @Test
        @DisplayName("躲闪10点(恒等递减)→躲闪几率0.10")
        void 躲闪10点_恒等递减_应返回0点10() {
            派生属性计算器.躲闪几率计算器 计算器 = new 派生属性计算器.躲闪几率计算器();
            属性快照 属性 = 创建属性快照(0, 0, 10, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.10, 结果, 0.000001);
        }

        @Test
        @DisplayName("躲闪0点→躲闪几率0")
        void 躲闪0点_应返回0() {
            派生属性计算器.躲闪几率计算器 计算器 = new 派生属性计算器.躲闪几率计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("躲闪50点(减半递减)→有效25点→躲闪几率0.25")
        void 躲闪50点_减半递减_应返回0点25() {
            派生属性计算器.躲闪几率计算器 计算器 = new 派生属性计算器.躲闪几率计算器();
            属性快照 属性 = 创建属性快照(0, 0, 50, 0, 0);

            double 结果 = 计算器.计算(属性, 减半递减);

            assertEquals(0.25, 结果, 0.000001);
        }
    }

    @Nested
    @DisplayName("暴击期望增伤计算器")
    class 暴击期望增伤计算器组 {

        @Test
        @DisplayName("派生名应返回'暴击期望增伤'")
        void 派生名_应返回暴击期望增伤() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            assertEquals("暴击期望增伤", 计算器.派生名());
        }

        @Test
        @DisplayName("暴击几率50 暴击伤害100(恒等递减)→期望增伤0.50")
        void 暴击几率50_伤害100_恒等递减_应返回0点50() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 50, 100);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.50, 结果, 0.000001);
        }

        @Test
        @DisplayName("暴击几率0→期望增伤0")
        void 暴击几率0_应返回0() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 0, 100);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("暴击伤害0→期望增伤0")
        void 暴击伤害0_应返回0() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 50, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(0.0, 结果, 0.000001);
        }

        @Test
        @DisplayName("暴击几率100 暴击伤害200(减半递减)→有效50和100→期望增伤0.50")
        void 暴击几率100_伤害200_减半递减_应返回0点50() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 100, 200);

            double 结果 = 计算器.计算(属性, 减半递减);

            assertEquals(0.50, 结果, 0.000001);
        }

        @Test
        @DisplayName("归零递减→期望增伤0")
        void 归零递减_应返回0() {
            派生属性计算器.暴击期望增伤计算器 计算器 = new 派生属性计算器.暴击期望增伤计算器();
            属性快照 属性 = 创建属性快照(0, 0, 0, 50, 100);

            double 结果 = 计算器.计算(属性, 归零递减);

            assertEquals(0.0, 结果, 0.000001);
        }
    }

    @Nested
    @DisplayName("边界值与一致性")
    class 边界值与一致性 {

        @ParameterizedTest
        @CsvSource({"0.0, 0.0", "20.0, 0.10", "40.0, 0.20", "100.0, 0.50", "200.0, 1.00"})
        @DisplayName("全能增伤比例应与全能值线性相关")
        void 全能增伤比例_应线性相关(double 全能, double 期望增伤) {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(全能, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertEquals(期望增伤, 结果, 0.000001);
        }

        @Test
        @DisplayName("Infinity全能应传播Infinity")
        void Infinity全能_应传播Infinity() {
            派生属性计算器.全能增伤比例计算器 计算器 = new 派生属性计算器.全能增伤比例计算器();
            属性快照 属性 = 创建属性快照(Double.POSITIVE_INFINITY, 0, 0, 0, 0);

            double 结果 = 计算器.计算(属性, 恒等递减);

            assertTrue(Double.isInfinite(结果));
        }

        @Test
        @DisplayName("全能增伤与全能减伤系数应一致(均为0.005)")
        void 全能增伤与减伤系数_应一致() {
            属性快照 属性 = 创建属性快照(40, 0, 0, 0, 0);
            派生属性计算器.全能增伤比例计算器 增伤计算器 = new 派生属性计算器.全能增伤比例计算器();
            派生属性计算器.全能减伤比例计算器 减伤计算器 = new 派生属性计算器.全能减伤比例计算器();

            double 增伤 = 增伤计算器.计算(属性, 恒等递减);
            double 减伤 = 减伤计算器.计算(属性, 恒等递减);

            assertEquals(增伤, 减伤, 0.000001);
        }
    }

    /**
     * 创建属性快照，仅设置5个派生计算器关心的字段。
     * 参数顺序：全能, 吸血, 躲闪, 法术暴击几率, 法术暴击伤害
     */
    private 属性快照 创建属性快照(double 全能, double 吸血, double 躲闪, double 法术暴击几率, double 法术暴击伤害) {
        return 属性快照.创建(
                1000, 1.0, 0, 0, 0, 0, 0, 0, 0,
                法术暴击几率, 法术暴击伤害, 0,
                全能, 吸血, 躲闪, 0, 0
        );
    }
}
