# 效果调度服务实现测试报告

## 基本信息

|项目|内容|
|---|---|
|实现文件|`src/main/java/mljy/业务层/效果调度服务实现.java`|
|测试文件|`src/test/java/mljy/业务层/效果调度服务实现测试.java`|
|测试框架|JUnit 5 + Mockito|
|针对性结果|10 个既有用例；本轮只增加结构化生命周期 debug，2026-07-15 定向执行被工程现有 `compileJava` 基线错误阻断|

## 覆盖范围

|场景|验证点|
|---|---|
|效果到期|依次调用 `on到期`、`on移除` 并删除数据|
|玩家清空|每个效果只调用一次 `on移除`|
|高层覆盖|旧处理器移除一次，新处理器添加一次|
|低层覆盖|层数和到期时间均保持不变|
|同层覆盖|不得延长到期时间|
|重复刷新|到期效果只触发一次到期/移除并删除一次|
|带原因清理|玩家清理携带原因并删除效果|
|真实护盾处理器到期|粒子任务只取消一次|

## 运行结果

```text
定向测试在 `compileJava` 阶段被工程中多个既有类型/包缺失阻断，JUnit 未启动。
本轮未运行全量测试，符合 FP-02 收口指令。
```
