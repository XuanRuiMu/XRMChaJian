# 技能按键绑定监听器测试报告

## 基本信息

|项目|内容|
|---|---|
|实现文件|`src/main/java/mljy/表现层/监听器/技能按键绑定监听器.java`|
|测试文件|`src/test/java/mljy/表现层/监听器/技能按键绑定监听器测试.java`|
|测试结果|通过36/失败0/跳过0|

## 本次回归

玩家左键输入与后续 `EntityDamageByEntityEvent` 使用不同事件对象。测试验证明确输入创建的玩家目标凭证可把两次入口绑定到同一根事件；同一最终伤害事件在技能入口重复读取时仍复用同一根标识。凭证按玩家隔离、命中后一次性消费，重复输入不串联。

## 覆盖范围

- 技能槽位、左键空气/方块、空手、右键、非技能物品。
- 会话、快照、技能定义缺失和非玩家攻击者。
- 玩家目标凭证的跨入口根事件关联。
- 事件取消配置与根事件不提前结束。

## 验证命令

`:XRM:test -PrunTests=true --tests mljy.表现层.监听器.技能按键绑定监听器测试 --rerun-tasks`

结果：36 个测试全部通过。
